# Enable hoisting to fix Docker volume mount issues
# This copies binaries into each workspace's node_modules instead of symlinking
# Required for pnpm to work correctly inside Docker containers with volume mounts
shamefully-hoist=true
