{
  "name": "saas-kit",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "setup": "pnpm i && pnpm run build:logger && pnpm run build:data-ops",
    "build:data-ops": "pnpm run --filter data-ops build",
    "build:logger": "pnpm run --filter @repo/logger build",
    "dev:yeko-core": "pnpm run --filter yeko-core dev",
    "deploy:yeko-core": "pnpm run build:data-ops && pnpm run build:logger && pnpm run --filter yeko-core deploy",
    "dev:yeko-school": "pnpm run --filter yeko-school dev",
    "deploy:yeko-school": "pnpm run build:data-ops && pnpm run build:logger && pnpm run --filter yeko-school deploy",
    "dev:yeko-teacher": "pnpm run --filter yeko-teacher dev",
    "deploy:yeko-teacher": "pnpm run build:data-ops && pnpm run build:logger && pnpm run --filter yeko-teacher deploy",
    "dev:data-service": "pnpm run --filter data-service dev",
    "deploy:data-service": "pnpm run build:data-ops && pnpm run build:logger && pnpm run --filter data-service deploy",
    "dev:queue-worker": "pnpm run --filter @repo/queue-worker dev",
    "deploy:queue-worker": "pnpm run build:data-ops && pnpm run --filter @repo/queue-worker deploy",
    "typecheck": "pnpm run --filter \"*\" typecheck || echo \"No typecheck script found\"",
    "lint": "pnpm run --filter \"*\" lint:fix || echo \"No lint script found\"",
    "lint:check": "pnpm run --filter \"*\" lint || echo \"No lint script found\"",
    "test": "pnpm run --filter \"*\" test || echo \"No test script found\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.28.2",
  "devDependencies": {
    "@praha/byethrow-docs": "^0.9.0",
    "@types/node": "catalog:",
    "knip": "^5.83.1",
    "lefthook": "^2.0.16",
    "typescript": "catalog:"
  },
  "dependencies": {
    "@praha/byethrow": "^0.9.0"
  }
}
