{
  "name": "saas-kit",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "setup": "pnpm i && pnpm run build:data-ops",
    "build:data-ops": "pnpm run --filter data-ops build",
    "dev:yeko-core": "pnpm run --filter yeko-core dev",
    "deploy:yeko-core": "pnpm run build:data-ops && pnpm run --filter yeko-core deploy",
    "dev:data-service": "pnpm run --filter data-service dev",
    "deploy:data-service": "pnpm run build:data-ops && pnpm run --filter data-service deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.22.0",
  "devDependencies": {
    "@types/node": "^22.18.12",
    "typescript": "^5.9.3"
  }
}
